"use client";

import { AnalyticsView } from "@/components/analytics/AnalyticsView";

export default function ManagerReportsPage() {
  return (
    <AnalyticsView
      title="التقارير"
      subtitle="تحليلات كاملة لكل الشركة — مع إمكانية التصدير"
    />
  );
}